using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using FindandReplaceSql.Models;

namespace FindandReplaceSql.Modules
{
    public class ColoredStringBuilder
    {
        public ColoredStringBuilder()
        {
            
        }

        private string TxtToConvet
        {
            get; set;
        }
        /*
        private LineCharacter BuildCharacter(char letter, int quoteOccur, int ampOccur)
        {
            if ( letter.Equals('"'))
            {
                return new LineCharacter(letter, Color.Coral);
            }
            else if (letter.Equals('&') && quoteOccur%2==0 )
            {
                return new LineCharacter(letter, Color.Black);
            }
            else
            {
                if(quoteOccur % 2 == 0 && ampOccur % 2 == )
            }
        }
         * */

    }
}
